# Default batch size for paginated annotation share endpoints.
# Authenticated list endpoints default to None (no limit) to preserve existing behaviour;
# this value is used only as the share-endpoint default.
ANNOTATIONS_PAGE_SIZE = 50
